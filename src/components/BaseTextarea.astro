---
const { name, placeholder = "", required = false } = Astro.props;
---

<textarea 
    class="textarea textarea-ghost text-base-200 focus:text-base-200 bg-gradient-to-r from-blue-50 to-purple-50 w-full"
    name={name}
    placeholder={placeholder}
    required={required}
/>